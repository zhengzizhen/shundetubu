import NEOneLoginIframe from '@Src/main/iframe';
import { DomNode } from '@Src/main/utils/domNode';
import type { LoginButtonStyles } from '@Src/main/types';
declare class LoginButton extends DomNode {
    private readonly iframe;
    readonly loginBtn: HTMLButtonElement;
    constructor(container: HTMLElement, iframe: NEOneLoginIframe);
    setLoginBtnLoading(loading: boolean): void;
    updateText(text?: string): void;
    updateStyles(styles?: LoginButtonStyles): void;
    private onLoginBtnClick;
    bindEvent(): void;
    removeAllEventListener(): void;
}
export default LoginButton;
