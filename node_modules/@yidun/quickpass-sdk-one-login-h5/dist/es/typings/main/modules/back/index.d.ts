import NEOneLoginIframe from '@Src/main/iframe';
import { DomNode } from '@Src/main/utils/domNode';
import { Mode } from '@Src/main/types';
declare class Back extends DomNode {
    private readonly iframe;
    readonly backBtnImg: HTMLImageElement;
    readonly backText: HTMLSpanElement;
    readonly backWrapper: HTMLElement;
    constructor(container: HTMLElement, iframe: NEOneLoginIframe);
    updateDisplayStatus(mode?: Mode): void;
    updateBackText(text?: string): void;
    private sendCloseMsg;
    bindEvent(): void;
    removeAllEventListener(): void;
}
export default Back;
