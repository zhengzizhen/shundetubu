import { ToastStyles } from '@Src/main/types';
import { Dispose } from '@Src/main/utils/dispose';
import ToastItem from './toast-item';
export declare type Position = 'center';
declare class Toast implements Dispose {
    private toasts;
    private container;
    constructor(container: HTMLElement);
    show(html: string | HTMLElement, position: Position, timeout?: number, styles?: ToastStyles): ToastItem;
    close(toastItem?: ToastItem): void;
    dispose(): void;
}
export default Toast;
