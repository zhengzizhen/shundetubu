import NEOneLoginIframe from '@Src/main/iframe';
import { DomNode } from '@Src/main/utils/domNode';
import type { SwitchButtonStyles } from '@Src/main/types';
declare class SwitchButton extends DomNode {
    private readonly iframe;
    readonly switchTextBtn: HTMLSpanElement;
    constructor(container: HTMLElement, iframe: NEOneLoginIframe);
    updateText(text?: string): void;
    updateStyles(styles?: SwitchButtonStyles): void;
    private onSwitchBtnClick;
    private bindEvent;
    removeAllEventListener(): void;
}
export default SwitchButton;
