import { DomNode } from '@Src/main/utils/domNode';
import { ToastStyles } from '@Src/main/types';
import { Position } from '.';
declare class ToastItem extends DomNode {
    timer: any;
    readonly position: Position;
    constructor(container: HTMLElement, html: string | HTMLElement, position?: Position, styles?: ToastStyles);
    dispose(): void;
    removeAllEventListener(): void;
}
export default ToastItem;
