import NEOneLoginIframe from '@Src/main/iframe';
import { DomNode } from '@Src/main/utils/domNode';
import type { PolicyDefine, PolicyTarget, PolicyStyles } from '@Src/main/types';
declare class Policy extends DomNode {
    private readonly iframe;
    readonly policy: HTMLDivElement;
    readonly checkbox: HTMLInputElement;
    constructor(container: HTMLElement, iframe: NEOneLoginIframe);
    updatePolicy(policy?: PolicyDefine[], target?: PolicyTarget): void;
    updateStyles(styles?: PolicyStyles): void;
    bindEvent(): void;
    removeAllEventListener(): void;
}
export default Policy;
