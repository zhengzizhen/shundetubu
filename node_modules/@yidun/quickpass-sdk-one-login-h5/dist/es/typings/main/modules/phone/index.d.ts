import NEOneLoginIframe from '@Src/main/iframe';
import { DomNode } from '@Src/main/utils/domNode';
import type { PhoneStyles, PhoneInputStyle } from '@Src/main/types';
declare class Phone extends DomNode {
    private readonly iframe;
    readonly doms: HTMLSpanElement[];
    readonly virtualInputDom: HTMLInputElement;
    inputStyle: PhoneInputStyle;
    values: number[];
    constructor(container: HTMLElement, iframe: NEOneLoginIframe);
    updateValue(values: number[]): void;
    updateStyles(styles?: PhoneStyles): void;
    updateInputStyle(style: PhoneInputStyle): void;
    bindEvent(): void;
    updateCurInputPos(pos?: number): void;
    removeAllEventListener(): void;
}
export default Phone;
