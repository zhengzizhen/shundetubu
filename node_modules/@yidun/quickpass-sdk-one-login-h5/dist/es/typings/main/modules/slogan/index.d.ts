import NEOneLoginIframe from '@Src/main/iframe';
import { DomNode } from '@Src/main/utils/domNode';
import type { SloganStyles } from '@Src/main/types';
declare class Slogan extends DomNode {
    private readonly iframe;
    readonly slogan: HTMLSpanElement;
    constructor(container: HTMLElement, iframe: NEOneLoginIframe);
    getAppName(name?: string): string;
    updateAppName(name?: string): void;
    updateStyles(styles?: SloganStyles): void;
    removeAllEventListener(): void;
}
export default Slogan;
