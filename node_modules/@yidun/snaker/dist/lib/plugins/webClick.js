"use strict";
exports.__esModule = true;
var utils_1 = require("./utils");
function createWebClick(snaker, genExtraData) {
    function handleClick(ev) {
        var el = ev.target;
        var xpath = utils_1.getElementXPath(el);
        var extraData = {};
        if (genExtraData !== undefined) {
            Object.assign(extraData, genExtraData(ev));
        }
        snaker.trackAsync('webClick', location.pathname, xpath, extraData);
    }
    function start() {
        document.addEventListener('click', handleClick);
    }
    return {
        start: start
    };
}
exports["default"] = createWebClick;
//# sourceMappingURL=webClick.js.map