import { Options } from '../index';
interface VuePluginOptions extends Options {
    router: any;
    webClick: boolean;
}
declare const _default: {
    install(Vue: any, pluginOptions: VuePluginOptions): void;
};
export default _default;
