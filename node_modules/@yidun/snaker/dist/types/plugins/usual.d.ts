import Snaker, { Options } from '../index';
interface UsualPluginOptions extends Options {
    webClick: boolean;
}
declare const _default: {
    install(pluginOptions: UsualPluginOptions): Snaker;
};
export default _default;
